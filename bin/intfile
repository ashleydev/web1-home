#!/bin/sh

if [ -n "$1" ]
then
  #hd ~pubstock/interm/$1 | head
  file ~pubstock/interm/$1
else
  echo Purpose:  Runs the file identifier command on an interm file
  echo
  echo Usage $0: '[ABC-0123456789.tmp]'
fi
